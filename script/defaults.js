const rounded_line_style = {
    lineCap: "round",
    lineJoin: "round",
};

const defaults = {
    ...rounded_line_style,
    
    lineWidth: "2",
    filLStyle: "white",
    strokeStyle: "black",
    textAlign: "left",
    textBaseline: "center",
}

const ANGLE_IN_12 = Math.PI / 6;
