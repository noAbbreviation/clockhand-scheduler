const rounded_line_style = {
    lineCap: "round",
    lineJoin: "round",
};

const defaults = {
    ...rounded_line_style,
    
    lineWidth: "2",
    font: "2.5em monospace",
    fillStyle: "white",
    strokeStyle: "black",
    textAlign: "left",
    textBaseline: "bottom",
}

const ANGLE_IN_12 = Math.PI / 6;
